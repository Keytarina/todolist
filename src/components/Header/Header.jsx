import StatusFilter from "../StatusFilter/StatusFilter";
import "./Header.scss";

const Header = () => {
	return (
		<header className="Header">
			<StatusFilter />
		</header>
	);
};

export default Header;
